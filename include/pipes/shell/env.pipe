---
version: 0.0.1

public:
  env:
    pipe:
      - env::var:
          when: "@?{var}"
          shell:
            run: "printf $@{var}"
      - env::save:
          when: "@?{save}"
          shell:
            run: "export @{save}=$!!"
